require "hanoi.rb"

describe "#move" do
    context "when the game is not won" do
              it "gets a move from the user" do
             expect(move).to (include?(gets))
             end
    end

    context "when the game is won" do
        it "does not promt the user for input"
        expect(move).to 
    end
end 